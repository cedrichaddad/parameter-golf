# Proposal Feature Status

This is the handoff table for local proposal work. It intentionally separates implementation from H100 validation and final BPB evidence.

| Feature | Status | Local Evidence | H100/BPB Evidence Needed |
|---|---:|---|---|
| Quant layout compiler | Implemented and locally tested | `pg-quant` manifest/CRC/kernel ID tests; strict loader tests | CUDA pack/dequant microbench |
| Artifact-lab bit allocation | Local proxy | `pg-local artifact-lab` reports mixed bit-allocation byte estimates and optional mini-BPB from explicit byte fixtures | Final export byte proof and post-export full-validation BPB |
| ModelGolf constraint compiler | Local planner plus Pack/LQER/CacheGolf/DeltaGolf/TrainGolf compiler slices | `pg-local modelgolf plan` emits ResourceContract with bytes/memory/latency/quality/training-time/energy-proxy constraints, a `cost_model` planning report, typed artifact IR, exact-byte Pack/LQER mixed-precision DP, CacheGolf KV planner, DeltaGolf allocation, TrainGolf objective, Optimizer/Comm compiler report, KernelForge contracts, Wind Tunnel Pareto summary, ScaleGolf cross-track matrix, and a fail-closed `release_readiness` gate; `modelgolf scale-plan` emits PG-Lite, ParameterGolf, DeltaGolf, LongContext, and ScaleGolf rows that all share the same resource contract/model IR/artifact compiler/runtime planner/evaluator/cost-model vocabulary; `modelgolf release-check --release-evidence <json>` validates spec/runtime-bound measured evidence, artifact SHA-256, SHA-256 verified per-pillar source reports including resource_cost wall-power telemetry, optimizer_comm distributed/NCCL telemetry, selected cache policy, selected delta names, kernel IDs, and recomputed Pack BPB delta, and validates measured resource-cost budget fit plus optimizer/communication parity/timing before marking pillars non-blocking; every release summary boolean/metric must match a digested JSON source-report claim; `modelgolf pack` emits `quality_comparison`, a deterministic equal-byte local proxy table for uniform Q4, best uniform no-LQER, exact mixed no-LQER, selected mixed+LQER, and LQER control/fallback plans; `modelgolf pack --proof-artifact` exports a deterministic local quant artifact through `pg-quant`, strict-reloads it against the requested `QuantSpec`, and reports deterministic fixture bytes plus finite pre/post-reload smoke losses for format/reload evidence; `modelgolf pack --quality-calibration` consumes measured group/role quality-loss points, scales Pack surrogate losses before DP selection, and reports fit factors/error; `modelgolf pack-experiment` exports, strict-reloads, and scores the plan's LQER++ table on a bounded deterministic small model with uniform Q4, mixed Q4/Q6, mixed+LQER top-3, and deterministic random-LQER control rows; `modelgolf pack` emits selected LQER residual proofs with rank-r error reduction and activation/logit bound reduction; `pg-quant` now enforces selective `lqer.top_k` by rank-r captured residual energy per actual requested-rank artifact byte, records selected LQER groups in artifact metadata, and uses exact truncated-SVD factors for small residual matrices; `pg-kernels` includes a tiled output CE CPU reference with full-logit loss/`dH`/`dW` parity tests, `modelgolf kernel-plan` emits an exact tiled CE local proof with loss/`dH`/`dW` parity against full logits, and `modelgolf kernel-experiment` reports full-logit vs tiled logit-free CE parity, CE scratch reduction, and local CPU timing; `pg-kernels` also includes a packed CacheGolf K/V format with per-channel K, per-token V, on-the-fly dequant-attention parity and perturbation-bound tests; `modelgolf cache-plan` emits selected-policy proof plus residual-sketch, sink/recent eviction, and contract-budget long-context memory scaling planner rows; `pg-kernels` also includes a DeltaGolf weighted low-rank Kronecker-curvature CPU reference, `modelgolf delta-plan` emits selected low-rank proofs with deterministic full updates and positive curvature, static score-first legality audits, and deterministic equal-byte domain proxy rows, and `pg-kernels` includes a TrainGolf stop-gradient quantization-distance regularizer/export-gap-bound reference; `modelgolf train-plan` emits a local regularizer/export-gap proof; `modelgolf plan` emits `optimizer_comm` with sharded Parallel Muon ring-collective byte estimates, graph/shadow/overlap contract flags, and a deterministic reduce-scatter/local-update/all-gather equivalence proof for shard-separable updates; `pg-model` exposes `GradBuffers::add_artifact_regularization` to accumulate TrainGolf regularizer gradients on CPU; `pg-train` can schedule that regularizer in non-CUDA train runs with result/audit counters | Held-out calibrated quality models, final trained export/BPB, long-context cache eval, production-scale randomized/exact LQER validation, measured delta full-update/curvature estimation and training evidence, TrainGolf GPU/backend integration and post-export comparisons, measured wall-power/energy telemetry, NCCL distributed optimizer timing/parity, CUDA/Metal kernel lowering with parity/perf, fresh profiler traces, and per-track measured quality/runtime/energy or dollar evidence for ScaleGolf claims |
| Compile-time CUDA arch metadata | Implemented and compile-checked | `PG_COMPILED_CUDA_ARCHES`, `PG_COMPILED_ARCH_PROFILE`, `cfg(pg_cuda_arch_sm90)` compile with `--features cuda` | Build log from final H100 image |
| Quant CUDA pack/dequant entrypoints | Implemented as optional symbols | `quant_pack.cu` compiles under CUDA feature check | Perf and parity microbench on H100 |
| Strict artifact manifest validation | Implemented and locally tested | malformed JSON, missing CRC, QuantSpec mismatch, strict export reload smoke | Final record export proof |
| BigramHash fused merge | Implemented and locally tested | CPU fused forward/backward reference, finite-difference tests, CUDA symbol stability test | CUDA parity/perf on H100; BPB if enabled in a variant |
| #2135 BigramHash claim | Scoped out | Audit keeps Bigram disabled for #2135 unless explicitly configured | None unless a new Bigram variant is proposed |
| Adjacent SparseAttnGate/XSA fusion | Implemented and scoped | CPU composite parity and audit `xsa_fusion_kind` tests | H100 perf/parity for active profile |
| True XSA-inside-SDPA | Local reference only, not record-active | CPU semantic forward/backward parity and optional naive F32 CUDA reference wrapper; audit still reports `true_xsa_in_attention_fusion=false` | Wire into `pg-model` for XSA layers, then CUDA parity/perf on H100 |
| Exact recurrent boundary fusion | Implemented in runtime surface | Audit distinguishes exact boundary fusion from ST speed probes | H100 exact active-step timing |
| Persistent-CTA block backward | Not implemented | Audit reports inactive/unvalidated | Exact-gradient kernel and H100 active-window improvement |
| Full train-step CUDA graph | Not implemented | Audit keeps claim false | Graph capture including data materialization, loss, backward, updates |
| Final record BPB | Not validated | None locally | Full train/eval/export and 3 seeds on canonical CaseOps |
| Artifact budget | Locally hardened, not final | Strict metadata and byte-estimate tests | Final compressed model/code byte proof from record run |
| PG-Lite | Local proxy only | `pg-local lite run` reports `proxy_only=true`, local BPB, wall time, actual compact `model.pglite.bin` artifact bytes plus debug JSON, loadable artifact manifests, and budget flags; `pg-local lite suite` runs all checked-in configs and writes summary JSON/Markdown | None; PG-Lite is not the official leaderboard benchmark |
| PG-Lite Metal backend | Locally validated on this host | `pg-local` parses `metal_apple`, rejects it by default unless the feature/runtime probe passes, and allows explicitly labeled CPU fallback with requested/execution backend fields. `pg-local backend-check` verifies the checked-in `pg_lite_ngram.metal` source, per-symbol kernel contracts, offline compiler availability, and feature-gated Rust runtime availability. The source plus executor cover add-one initialization, atomic training, u32-to-u16 compaction, row-sum precompute, presummed loss, fused loss+reduction, legacy parity loss, and sequential score-first eval using the same 64-bit FNV context hash as CPU. `pg-local lite benchmark` now runs CPU and Metal with fallback disabled; the checked proof shows Metal execution is available and BPB parity holds on the tiny PG-Lite fixture. | Larger local corpora and wall-power/thermal runs if PG-Lite performance claims matter |
| Wind Tunnel | Local reviewer packet implemented | `pg-local wind-tunnel` reports `estimate_only=true`, stage estimates, bottleneck hints, and a spec-derived operation DAG; `pg-local wind-tunnel-suite` ingests JSON and log traces, distinguishes total-only traces from nonzero stage-attribution traces, and writes checked-in calibration summaries under `records/wind_tunnel/`; `pg-local wind-tunnel report` now bundles trace index, deduped corpus, calibration, scenario, suite, train-only holdout predictions with per-run error and Spearman rank correlation, JSON, and Markdown for external review | Fresh H100 profiler trace and full train/eval timing |
| Historical H100 trace index | Local evidence inventory | `pg-local trace-index` scans the broader records tree, extracts timing/audit fields from JSON/log/markdown artifacts, and writes `records/wind_tunnel/all_records_trace_index.json`; current index finds 109 trace-like files, 13 timed traces, best exact/#2135 timing at 127.109924 ms/step, and best active recurrent timing at 155.576088 ms/active step | New traces still required for final validation and richer stage attribution |

## Handoff Notes

- Keep `frontier_2135_audit_target.toml` as the primary competitive target.
- Keep speed probes and straight-through recurrence labeled as diagnostic until full BPB validates them.
- Do not claim record-active true XSA-inside-SDPA, persistent-CTA block backward, full train-step graph, record readiness, or leaderboard win from local tests alone.
- Do not cite Wind Tunnel estimates as measured H100 timing.
- Use `pg-local lite suite` and `pg-local wind-tunnel-suite` as local regression
  surfaces before spending Modal credits, but keep both labeled as local proxy
  evidence.
- Use `pg-local proof-bundle` for handoff packets; it now includes PG-Lite
  benchmark/suite summaries, backend readiness probes, and a Wind Tunnel
  reviewer packet in addition to the single-run checks.
- Treat `evidence_manifest.json` as the top-level handoff index; it explicitly
  separates local evidence from missing H100/BPB/artifact proof.
- Do not compare PG-Lite proxy BPB directly against official FineWeb/H100 leaderboard BPB.
- Do not call a `metal_apple` CPU-fallback PG-Lite run a Metal result.
- The next paid runs should be H100 validation only after artifact budget and local checks are green.
